package ${packageName}.${moduleName}.exception;

/**
 * @description ${table.description}
 * @author ${author}
 * @date ${now}
 * @version ${version}
 */
public class ${table.className}Exception extends RuntimeException{

    public ${table.className}Exception(String message) {
        super(message);
    }
    
}
